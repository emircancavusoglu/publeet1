import 'package:flutter/material.dart';
class AnnounceAdmin extends StatelessWidget {
  const AnnounceAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Duyurularım"),
      ),
    );
  }
}


